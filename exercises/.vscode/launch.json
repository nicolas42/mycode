{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/exercises/a.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/exercises",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",  // macOS uses LLDB by default
            "preLaunchTask": "build"
        }
    ]
}