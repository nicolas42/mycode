{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/test/a.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/test",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",  // macOS uses LLDB by default
            "preLaunchTask": "build"
        }
    ]
}